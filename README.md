# Þróunarbók

Encrypted development-portfolio dashboard for the Ísfast development team. Published nightly from the Mac mini (`refresh_project_costs.py`). Only `index.html` + `isfast-team.enc` — no plaintext data in this repo.
